name: CI Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup Node
      uses: actions/setup-node@v3

    - name: Install Dependencies
      run: echo "No dependencies required"

    - name: Build
      run: echo "Building Website..."

    - name: Test
      run: echo "Testing Website..."

    - name: Complete Job
      run: echo "CI Pipeline Successful"